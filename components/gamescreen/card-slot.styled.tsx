import { styled, View } from "tamagui";

const CardSlotStyled = styled(View, {
  minWidth: "$8",
  minHeight: "$11",
  borderColor: "$red9",
  padding: 1,
  backgroundColor: "$gray9",
  borderRadius: 4,
  alignItems: "center",
  justifyContent: "center",
  borderWidth: 3,
});

export default CardSlotStyled;
